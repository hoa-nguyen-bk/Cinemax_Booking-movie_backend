const {Ticket} = require('../../models');
const express = require("express");
const createTicket = async (userId, movieId) => {
  try {
   
  } catch (error) {
    
  }
}
module.exports={
  createTicket
}